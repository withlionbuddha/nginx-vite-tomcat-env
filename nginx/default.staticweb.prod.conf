server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API integration is provided by the integrated Compose configurations.
    location /api/ {
        return 404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
